import "./pill-menu.css"

export default function PillMenu({primaryLabel = "Save", secondaryLabel = "Cancel", primaryColor = "#252525"}) {
    return (
        <div className="pill-menu" style={{backgroundColor: primaryColor}}>
            
        </div>
    )
}